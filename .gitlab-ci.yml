stages:
  - test

default:
  tags:
    - docker


unit-test:
  stage: test
  script:
    - pip install pytest pytest-cov
    - python -m pytest --cov-report=html --cov=src .
  artifacts:
    paths:
      - coverage
    expire_in: 30 days