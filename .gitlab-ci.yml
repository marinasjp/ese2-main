image: python:3.8

stages:
  - test

before_script:
  - pip install numpy

unit-test:
  stage: test
  script:
    - pip install scipy
    - pip install pytest pytest-cov
    - python -m pytest --import-mode=append ./nanoindentation-dashboard/src/Processor/Processes/
